# Desktop

Electron + React desktop app for FrameLab.

## Development

```bash
cd desktop
npm install
npm run dev
```

The React renderer expects the backend at:

```text
http://127.0.0.1:8000/api/v1
```
